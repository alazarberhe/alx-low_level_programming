#include <stdio.h>

/**
 * main - entry point
 * 
 * Description: Print numbers 0 to 9 on one line
 * Return: 0
 */
int main(void)
{
int num;

for (num = 0; num < 9; ++num)
putchar(('0' + num);

putchar('\n');

return (0);
}

